title: JALT PIE SIG
description: Performance in Education Special Interest Group
url: "https://jaltpiesig.org"

theme: jekyll-theme-primer # Or build a completely custom layout using HTML/CSS

plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Force Jekyll to process image assets inside _posts subfolders
include:
  - _posts

# For your local testing environment
exclude: [Gemfile, Gemfile.lock, node_modules]